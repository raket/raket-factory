import React from 'react'
import ReactDOM from 'react-dom'
import Button from './src/Button'

ReactDOM.render(
<div>
  <Button text={'I\'m a button'}/>
  <Button text={'I\'m an other button'}/>
</div>,
document.getElementById('root')
);
